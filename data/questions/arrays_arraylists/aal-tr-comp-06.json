{
  "id": "aal-tr-comp-06",
  "type": "TRACING",
  "difficulty": 3,
  "prompt": "What does this program print?",
  "code": "import java.util.*;\npublic class Conversion {\n    public static void main(String[] args) {\n        int[] arr = {4, 2, 7, 1};\n        Integer[] boxed = new Integer[arr.length];\n        for (int i = 0; i < arr.length; i++) boxed[i] = arr[i];\n        List<Integer> list = new ArrayList<>(Arrays.asList(boxed));\n        Collections.sort(list);\n        int[] sorted = new int[list.size()];\n        for (int i = 0; i < sorted.length; i++) sorted[i] = list.get(i);\n        System.out.println(Arrays.toString(sorted));\n        System.out.println(list.contains(7));\n    }\n}",
  "answer": "[1, 2, 4, 7]\ntrue",
  "explanation": "The code converts int[] → Integer[] → List, sorts it, converts back to int[]. Sorted: [1,2,4,7]. list.contains(7) uses .equals() for Integer comparison → true.",
  "alternatives": ["[1, 2, 4, 7]\ntrue\n"]
}
